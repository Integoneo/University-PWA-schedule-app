/**
 * changelog.ts — единственный источник истины о версиях приложения.
 *
 * Как добавить новую запись при релизе:
 *   1. Увеличь CURRENT_VERSION (semver: major.minor.patch)
 *   2. Раскомментируй шаблон ниже, заполни поля и добавь в начало массива
 *   3. Обновить app_data_version в store.ts при breaking changes стора
 *
 * Типы релизов:
 *   major — ломающие изменения, новый крупный функционал
 *   minor — новые функции без ломающих изменений
 *   patch — исправления, мелкие улучшения
 */

// WARN: Необходимо менять эту переменную при обновлении
export const CURRENT_VERSION = '1.0.0'
// WARN: Необходимо менять эту переменную при обновлении

export interface ChangelogEntry {
  version: string
  date: string
  type: 'major' | 'minor' | 'patch'
  features: string[]
  fixes: string[]
}

/**
 * История изменений — новые версии ВВЕРХУ массива.
 *
 * Шаблон для новой записи:
 * ─────────────────────────────────────────────────────
 * {
 *   version: '1.1.0',
 *   date: '2026-10-01',
 *   type: 'minor',
 *   features: [
 *     'Новая функция A',
 *     'Новая функция B',
 *   ],
 *   fixes: [
 *     'Исправлено: ...',
 *   ],
 * },
 * ─────────────────────────────────────────────────────
 */
export const changelogHistory: ChangelogEntry[] = [
  // Добавляй новые версии сюда ↓ (новые первыми)

]
